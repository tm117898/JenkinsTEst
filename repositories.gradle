def addBuildScriptRepos(RepositoryHandler handler) {
    handler.maven {
        url 'http://dev-pv.saicmotor.com:9090/nexus/content/groups/android_public/'
        credentials {
            username = 'superman'
            password = 'SupermanAA12**'
        }
    }
}
ext.addBuildScriptRepos = this.&addBuildScriptRepos


def addAllProjectsRepos(RepositoryHandler handler) {
    handler.maven {
        url 'http://dev-pv.saicmotor.com:9090/nexus/content/groups/android_public/'
        credentials {
            username = 'superman'
            password = 'SupermanAA12**'
        }
    }
    handler.flatDir dirs: rootProject.subprojects.collect { project -> "${project.projectDir}/libs" } + "$projectDir/libs"
}
ext.addAllProjectsRepos = this.&addAllProjectsRepos